{
    "_example_comment_dev_url": "Keep dev.url in sync with vite.config.ts server.port (default 5173; strictPort fails if that port is busy—free it or change both places).",
    "database": {
        "engine": "postgres",
        "provider": "postgres",
        "postgres": {
            "host": "127.0.0.1",
            "port": 5432,
            "database": "image_scoring",
            "user": "postgres",
            "password": "postgres",
            "ssl": {
                "enabled": false,
                "rejectUnauthorized": true
            },
            "pool": {
                "min": 0,
                "max": 10,
                "idleTimeoutMillis": 30000,
                "connectionTimeoutMillis": 10000
            }
        }
    },
    "dev": {
        "url": "http://localhost:5173"
    },
    "api": {
        "url": "http://127.0.0.1:7860"
    },
    "paths": {
        "remap_legacy_image_scoring_thumbnails": true,
        "thumbnail_base_dir": "../image-scoring-backend/thumbnails",
        "thumbnail_path_remap": [
            {
                "from": "../image-scoring/thumbnails",
                "to": "../image-scoring-backend/thumbnails"
            }
        ]
    },
    "_example_comment_sync": "File → Sync: set destinationRoot to your local photo library root (Windows path). Photos copy into destinationRoot/camera/lens/year/date.",
    "sync": {
        "destinationRoot": ""
    }
}
